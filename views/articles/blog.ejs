<%- include ("_header") %>

<div class="container">
	<section class="blog">
		<h1>Blog Articles</h1>
		<a href="articles/new" class="btn btn-success">New Article</a>

		<% articles.forEach(article => { %>
		<div class="article">
			<div class="article-body">
				<h4 class="article-title"><%= article.title %></h4>
				<div class="article-subtitle mb-2">
					<%= article.createdAt.toLocaleDateString() %>
				</div>
				<div class="article-text mb-2"><%= article.description %></div>
				<a href="articles/<%= article.slug %>" class="btn btn-outline">Continue Reading...</a>
				</form>
			</div>
		</div>
		<% }) %>
	</section>
</div>

<%- include ("_footer") %>
